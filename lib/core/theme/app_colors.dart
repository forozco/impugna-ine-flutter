import 'package:flutter/material.dart';

/// Colores oficiales del INE
class AppColors {
  // Colores primarios INE
  static const Color primary = Color(0xFFC42C7A); // Magenta INE
  static const Color secondary = Color(0xFF351C45); // Morado oscuro
  static const Color accent = Color(0xFFD5007F); // Rosa

  // Estados
  static const Color success = Color(0xFFC8F7DC); // Verde claro
  static const Color successDark = Color(0xFF155724);
  static const Color error = Color(0xFFDC3545); // Rojo
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF17A2B8);

  // Grises
  static const Color grey = Color(0xFF6C757D);
  static const Color greyLight = Color(0xFFE8E8E8);
  static const Color greyLighter = Color(0xFFF5F5F5);
  static const Color greyDark = Color(0xFF495057);

  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color inputBackground = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0xFFCED4DA);
  static const Color borderFocus = Color(0xFF80BDFF);

  // Text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF6C757D);

  // Special
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
